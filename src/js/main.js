// $("[role=\"tab\"]").children("a").click(function () {
//     const parent = $(this).parent()
//     if (!parent.attr("aria-selected")) {
//         $("[aria-selected]").attr("aria-selected", null);
//         parent.attr("aria-selected", true);
//         // TODO: Code that loads the respective page
//     }
// });